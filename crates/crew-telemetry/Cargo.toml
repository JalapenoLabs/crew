[package]
name = "crew-telemetry"
description = "Shared structured-logging (tracing) init and redaction for the crew binaries."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
tracing.workspace = true
tracing-subscriber.workspace = true
# Escape the event name correctly when injecting it into the JSON log output
# (issue #271); also parses the JSON output in tests.
serde_json.workspace = true
